import {
  SIMPLE_POST,
  PAY_PER_VIEW,
  PROFILE_GROWTH,
  LIKES,
  COMMENTS,
  SHARES,
  WEB_VISTS,
  ADD_TO_CART,
  SALES,
} from '../constants/FamepayConstants';

export const setObjectiveName = name => {
  if (name === '') {
  }
};

import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  ClaimPrize_claimButton: {
    margin: '30px',
    background: '#17bf17',
    '&:hover': {
      backgroundColor: '#0ca40c',
    },
  },
});
export { useStyles };

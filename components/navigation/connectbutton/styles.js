import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  connectButton: {
    backgroundColor: '#C80218',
    borderRadius: '0',
    color: 'white',
  },
});

export { useStyles };

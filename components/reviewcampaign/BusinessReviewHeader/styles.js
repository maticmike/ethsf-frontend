import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  BusinessReview_component_outline: {
    display: 'flex',
    gap: '20px',
  },
  BusinessReview_round_header: {
    borderRadius: '50%',
  },
  BusinessReview_margin_bottom_h1: {
    marginBottom: '6px',
  },
});

export { useStyles };

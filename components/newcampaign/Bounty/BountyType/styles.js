import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  BountType_heading_center: {
    textAlign: 'center',
  },
  BountyType_buttons_spacing: {
    display: 'flex',
    justifyContent: 'space-around',
    marginTop: '20%',
  },
});

export { useStyles };
